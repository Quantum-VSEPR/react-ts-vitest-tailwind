function App() {
  return (
    <div
      style={{
        display: "grid",
        placeItems: "center",
        height: "98vh",
      }}
    >
      <h2>
        React + TypeScript + Vitest = robust, type-safe code without extensive
        configuration
      </h2>
    </div>
  );
}

export default App;
